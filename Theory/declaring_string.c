#include<stdio.h>
int main(){
char str[]="MAni";
char *ptr="MAni";
return 0;
}
// we can use these two methods to decalare string



// difference between these two methods are  
// if we assign any string with the first method then we cannot cahnge the string
// if we assign any string using the 2nd method then we can change the string