#include<stdio.h>
int main(){
printf("The size of the integer in my architecture is %d\n",sizeof(int));
printf("The size of the float in my architecture is %d\n",sizeof(float));
printf("The size of the character in my architecture is %d\n",sizeof(char));
return 0;
}